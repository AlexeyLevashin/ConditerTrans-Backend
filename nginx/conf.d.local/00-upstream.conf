# Docker embedded DNS — re-resolve container IPs after restarts (avoids stale upstream 502).
resolver 127.0.0.11 valid=10s ipv6=off;

map $http_upgrade $connection_upgrade {
    default upgrade;
    '' close;
}
